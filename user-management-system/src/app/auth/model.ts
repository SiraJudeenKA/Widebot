/**
 * interface used to declare the type for login details
 */
export interface loginDetails {
    username: string;
    password: string;
}
/**
 * interface used to declare the type for the credentials value
 */
export interface credentialValue {
    role: string,
    credentials: boolean
}
